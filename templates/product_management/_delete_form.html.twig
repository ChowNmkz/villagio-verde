<form method="post" action="{{ path('app_product_management_delete', {'id': product.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
